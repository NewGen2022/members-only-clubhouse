<% layout('layout', { user: user }) %>

<!-- display messages only if user authenticated -->
<% if (isAuthenticated) { %> <%- include('messages') %> <% } else { %> <%-
include('welcome-page') %> <% } %>
